It supports API, Android UI, IOS UI automation;
Use PO design mode, pytest framework;
Supports data driven testing;

Usage:
1. create venv according to the requirements libs;
2. coding test case in test package, UI in sub dir;
3. execute run.py to run all cases under test dir;
